#include <stdio.h>
/**
 * main - Prints the alphabet.
 * Return: Always 0 (Success)
 */
int main(void)
{
	char k;
	
	k = 'a';
	while
		(k <= 'z') {
			putchar(k);
			k++;
		}
	putchar('\n');
	return (0);
}
